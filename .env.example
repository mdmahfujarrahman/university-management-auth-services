# Copy to .env and fill in. The server exits on startup without DATABASE_URI.
NODE_ENV=development
PORT=5000

# Local MongoDB, or an Atlas connection string.
DATABASE_URI=mongodb://localhost:27017/university-management

# Assigned to every student account created through /users/create-student.
DEFAULT_STUDENT_PASSWORD=student123
